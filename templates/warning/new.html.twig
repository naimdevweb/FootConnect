{# filepath: /c:/Users/Dev404/Documents/foot_connect/FOOT_CONNECT/templates/warning/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Nouvel avertissement{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<style>
    .fade-in {
        animation: fadeIn 0.6s ease-in-out;
    }
    .slide-up {
        animation: slideUp 0.5s ease-in-out;
    }
    .pulse {
        animation: pulse 2s infinite;
    }
    .hover-scale {
        transition: transform 0.3s ease;
    }
    .hover-scale:hover {
        transform: scale(1.03);
    }
    .form-input {
        transition: all 0.3s ease;
    }
    .form-input:focus {
        box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.3);
    }
    .dark .form-input:focus {
        box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.3);
    }
    .btn-gradient {
        background: linear-gradient(45deg, #3b82f6, #60a5fa);
        transition: all 0.3s ease;
    }
    .dark .btn-gradient {
        background: linear-gradient(45deg, #2563eb, #3b82f6);
    }
    .btn-gradient:hover {
        background: linear-gradient(45deg, #2563eb, #3b82f6);
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(59, 130, 246, 0.3);
    }
    .dark .btn-gradient:hover {
        background: linear-gradient(45deg, #1d4ed8, #2563eb);
        box-shadow: 0 10px 15px -3px rgba(37, 99, 235, 0.4);
    }
    .card {
        transition: all 0.3s ease;
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }
    .dark .card {
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.3), 0 4px 6px -2px rgba(0, 0, 0, 0.2);
    }
    .card:hover {
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    .dark .card:hover {
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
    }
    @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    @keyframes slideUp {
        from { transform: translateY(30px); opacity: 0; }
        to { transform: translateY(0); opacity: 1; }
    }
    @keyframes pulse {
        0% { box-shadow: 0 0 0 0 rgba(59, 130, 246, 0.4); }
        70% { box-shadow: 0 0 0 10px rgba(59, 130, 246, 0); }
        100% { box-shadow: 0 0 0 0 rgba(59, 130, 246, 0); }
    }
    
    .material-icons {
        font-family: 'Material Icons';
        font-weight: normal;
        font-style: normal;
        font-size: 24px;
        line-height: 1;
        letter-spacing: normal;
        text-transform: none;
        display: inline-block;
        white-space: nowrap;
        word-wrap: normal;
        direction: ltr;
        -webkit-font-feature-settings: 'liga';
        -webkit-font-smoothing: antialiased;
    }
</style>
{% endblock %}

{% block page_header %}
    <div class="sticky top-0 z-30 bg-white dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 shadow-sm backdrop-blur-sm bg-white/90 dark:bg-gray-800/90 transition-colors duration-200">
        <div class="container mx-auto px-4 py-3">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-2">
                    <a href="{{ path('app_warning_index') }}" class="p-2 -ml-2 text-gray-600 dark:text-gray-400 hover:text-gray-800 dark:hover:text-gray-200 transition-colors rounded-full hover:bg-gray-100 dark:hover:bg-gray-700">
                        <i class="fas fa-arrow-left"></i>
                    </a>
                    <a href="{{ path('app_warning_index') }}" class="text-gray-600 dark:text-gray-400 font-semibold text-lg hover:text-gray-800 dark:hover:text-gray-200 transition-colors">
                        Avertissements
                    </a>
                </div>
                <h1 class="text-xl font-bold text-gray-800 dark:text-gray-100 flex items-center transition-colors duration-200">
                    <div class="w-8 h-8 bg-amber-600 dark:bg-amber-700 rounded-full flex items-center justify-center mr-2 transition-colors duration-200">
                        <i class="fas fa-plus text-white text-sm"></i>
                    </div>
                    Nouvel avertissement
                </h1>
                <div></div>
            </div>
        </div>
    </div>
{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-gray-100 dark:from-gray-900 dark:to-gray-800 py-12 px-4 sm:px-6 lg:px-8 fade-in transition-colors duration-200">
    <div class="container mx-auto max-w-3xl">
        <div class="card bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden slide-up hover-scale transition-colors duration-200">
            <div class="bg-gradient-to-r from-amber-600 to-amber-700 dark:from-amber-700 dark:to-amber-800 px-6 py-6 rounded-t-xl transition-colors duration-200">
                <h1 class="text-2xl font-bold text-white flex items-center">
                    <i class="fas fa-exclamation-triangle text-amber-200 dark:text-amber-300 mr-3 transition-colors duration-200"></i>
                    Nouvel avertissement
                </h1>
                {% if user is defined %}
                <p class="text-amber-100 dark:text-amber-200 mt-1 flex items-center transition-colors duration-200">
                    <i class="fas fa-user text-sm mr-1"></i>
                    Pour l'utilisateur: <span class="font-medium ml-1">{{ user.pseudo }}</span>
                </p>
                {% endif %}
            </div>
            
            <div class="p-8">
                <div class="bg-blue-50 dark:bg-blue-900/30 rounded-lg p-4 mb-6 border-l-4 border-blue-500 dark:border-blue-600 transition-colors duration-200">
                    <p class="text-blue-800 dark:text-blue-300 text-sm flex items-center transition-colors duration-200">
                        <i class="fas fa-info-circle text-blue-600 dark:text-blue-400 mr-2 transition-colors duration-200"></i>
                        Veuillez décrire précisément la raison de cet avertissement pour une meilleure compréhension de l'utilisateur.
                    </p>
                </div>
                
                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                    <div class="slide-up" style="animation-delay: 0.1s">
                        {{ form_label(form.reason, 'Motif', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2 transition-colors duration-200'}}) }}
                        {{ form_widget(form.reason, {'attr': {'class': 'form-input block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-800 dark:text-gray-200 shadow-sm focus:border-blue-500 dark:focus:border-blue-400 focus:ring-0 transition-colors duration-200'}}) }}
                        {{ form_errors(form.reason, {'attr': {'class': 'text-red-500 dark:text-red-400 text-sm mt-1 transition-colors duration-200'}}) }}
                    </div>
                    
                    <div class="slide-up" style="animation-delay: 0.2s">
                        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2 transition-colors duration-200'}}) }}
                        {{ form_widget(form.description, {'attr': {'class': 'form-input block w-full rounded-lg border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-700 text-gray-800 dark:text-gray-200 shadow-sm focus:border-blue-500 dark:focus:border-blue-400 focus:ring-0 transition-colors duration-200', 'rows': 5}}) }}
                        {{ form_errors(form.description, {'attr': {'class': 'text-red-500 dark:text-red-400 text-sm mt-1 transition-colors duration-200'}}) }}
                        <p class="text-sm text-gray-500 dark:text-gray-400 mt-2 transition-colors duration-200">
                            <i class="fas fa-lightbulb text-yellow-500 dark:text-yellow-400 transition-colors duration-200"></i>
                            Soyez précis et factuel dans votre description. L'utilisateur doit comprendre précisément ce qui lui est reproché.
                        </p>
                    </div>
                    
                    <div class="pt-6 flex flex-col sm:flex-row justify-between items-center border-t border-gray-200 dark:border-gray-700 mt-8 slide-up gap-4 sm:gap-0 transition-colors duration-200" style="animation-delay: 0.3s">
                        <a href="{{ path('app_warning_index') }}" 
                           class="w-full sm:w-auto inline-flex justify-center items-center px-5 py-2.5 rounded-lg border border-gray-300 dark:border-gray-600 text-sm font-medium text-gray-700 dark:text-gray-300 bg-white dark:bg-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600 hover:text-gray-900 dark:hover:text-gray-100 transition-all duration-200 ease-in-out">
                            <i class="fas fa-arrow-left mr-2"></i>
                            Retour aux avertissements
                        </a>
                        <button type="submit" 
                                class="w-full sm:w-auto btn-gradient inline-flex justify-center items-center px-5 py-2.5 rounded-lg border border-transparent text-sm font-medium text-white focus:outline-none transition-all duration-200 ease-in-out">
                            <i class="fas fa-save mr-2"></i>
                            Enregistrer l'avertissement
                        </button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
        
        <div class="text-center mt-8 text-gray-500 dark:text-gray-400 text-sm fade-in transition-colors duration-200" style="animation-delay: 0.4s">
            <p>Centre de modération - FootConnect</p>
        </div>
    </div>
</div>

<!-- Navigation mobile -->
<nav class="md:hidden fixed bottom-0 left-0 right-0 bg-white dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 shadow-lg z-10 transition-colors duration-200">
    <div class="flex justify-around items-center py-2">
        <a href="{{ path('app_home') }}" class="flex flex-col items-center text-gray-600 dark:text-gray-400 p-2 transition-colors duration-200">
            <i class="fas fa-home text-xl"></i>
            <span class="text-xs mt-1">Accueil</span>
        </a>
        <a href="{{ path('app_discover') }}" class="flex flex-col items-center text-gray-600 dark:text-gray-400 p-2 transition-colors duration-200">
            <i class="fas fa-compass text-xl"></i>
            <span class="text-xs mt-1">Explorer</span>
        </a>
        <div class="relative">
            <a href="#" class="flex items-center justify-center p-3 bg-gradient-to-r from-blue-600 to-blue-700 dark:from-blue-700 dark:to-blue-800 text-white rounded-full shadow-md -mt-5 relative z-10 transition-colors duration-200">
                <i class="fas fa-plus"></i>
            </a>
            <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-12 h-12 bg-white dark:bg-gray-800 rounded-full -z-10 transition-colors duration-200"></div>
        </div>
        <a href="{{ path('app_moderation_dashboard') }}" class="flex flex-col items-center text-amber-600 dark:text-amber-400 p-2 relative transition-colors duration-200">
            <i class="fas fa-shield-alt text-xl"></i>
            <span class="text-xs mt-1">Modération</span>
            <div class="absolute -bottom-2 w-10 h-1 bg-amber-600 dark:bg-amber-400 rounded-full transition-colors duration-200"></div>
        </a>
        <a href="{{ path('app_profil') }}" class="flex flex-col items-center text-gray-600 dark:text-gray-400 p-2 transition-colors duration-200">
            <i class="fas fa-user text-xl"></i>
            <span class="text-xs mt-1">Profil</span>
        </a>
    </div>
</nav>
{% endblock %}