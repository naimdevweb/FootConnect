{# filepath: c:\Users\Dev404\Documents\foot_connect\FOOT_CONNECT\templates\security\reset_password\check_email.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé - FootConnect{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(10px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-fade-in {
            animation: fadeIn 0.6s ease-out forwards;
        }
    </style>
{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 dark:from-gray-900 dark:to-gray-800 py-12 px-4 sm:px-6 lg:px-8 flex flex-col items-center justify-center transition-colors duration-200">
    <div class="w-full max-w-md animate-fade-in">
        <div class="text-center mb-8">
            <div class="mx-auto h-16 w-16 bg-green-600 dark:bg-green-700 rounded-full flex items-center justify-center shadow-lg mb-4 transition-colors duration-200">
                <i class="fas fa-envelope-open-text text-white text-2xl"></i>
            </div>
            <h1 class="text-3xl font-bold text-gray-900 dark:text-white transition-colors duration-200">Email envoyé</h1>
            <p class="mt-2 text-gray-600 dark:text-gray-300 transition-colors duration-200">
                Si un compte existe avec cette adresse email, nous venons de vous envoyer un lien pour réinitialiser votre mot de passe.
            </p>
        </div>
        
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-6 sm:p-8 transition-colors duration-200">
            <div class="text-gray-700 dark:text-gray-300 space-y-4 transition-colors duration-200">
                <p class="flex items-start">
                    <i class="fas fa-info-circle text-blue-600 dark:text-blue-400 mt-1 mr-3 flex-shrink-0 transition-colors duration-200"></i>
                    <span>
                        Ce lien expirera dans 24 heures. Si vous ne recevez pas d'email, vérifiez votre dossier spam ou <a href="{{ path('app_reset_password_request') }}" class="text-blue-600 dark:text-blue-400 hover:underline transition-colors duration-200">essayez à nouveau</a>.
                    </span>
                </p>
            </div>
            
            <div class="mt-6">
                <a href="{{ path('app_login') }}" class="w-full flex justify-center items-center py-3 px-4 bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 font-medium rounded-lg hover:bg-blue-200 dark:hover:bg-blue-800/50 focus:outline-none focus:ring-2 focus:ring-blue-500 dark:focus:ring-blue-400 focus:ring-offset-2 shadow-sm transition-all duration-200">
                    <i class="fas fa-arrow-left mr-2"></i>
                    Retour à la page de connexion
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}