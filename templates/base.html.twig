<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FootBook - Réseau Social de Football</title>
    {% block stylesheets %}
  <link rel="stylesheet" href="{{ asset('styles/app.css') }}" />
    {% endblock %}
</head>

<body class="bg-gray-100 text-gray-800">
    <header>
        <nav class="bg-white shadow-md p-4">
            <div class="container mx-auto flex justify-between items-center">
                <a class="text-2xl font-semibold text-blue-600" href="{{ path('app_home') }}">FootBook</a>
                {% if app.user %}
               <h2  class="font-semibold text-lg text-gray-800">{{ app.user.pseudo}}</h2>
                {% endif %}
                
                  <input type="text" placeholder="Rechercher..." class="border rounded-full px-4 py-2 w-1/3 focus:outline-none">
                <div class="space-x-4">
                    <a class="text-gray-700 hover:text-blue-600" href="{{ path('app_home') }}">Accueil</a>
                    {% if app.user %}
                    <a class="text-gray-700 hover:text-blue-600" href="">Mon Profil</a>
                     <a class="text-gray-700 hover:text-blue-600" href="{{path('app_logout')}}">Deconnexion</a>
                    {% else %}
                    <a class="text-gray-700 hover:text-blue-600" href="{{path('app_login')}}">Connexion</a>
                    <a class="text-gray-700 hover:text-blue-600" href="{{path('app_register')}}">Inscription</a>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    
        {% block body %}
        {% endblock %}
    

    <footer class="bg-gray-900 text-white text-center py-4 mt-8">
        <p>&copy; 2025 FootBook</p>
    </footer>
</body>

</html>
